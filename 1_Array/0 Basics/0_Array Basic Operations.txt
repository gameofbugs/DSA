namespace DSA_Problems;

class Program
{
    static void Main(string[] args)
    {
        int[] array = { 1, 2, 3, 4 };
        //
        int[] arrray2 = new int[5];
        arrray2[0] = 1;
        arrray2[1] = 2;
        arrray2[2] = 3;
        arrray2[3] = 4;
        Console.WriteLine("Using for loop and array 1 before adding elements");
        for (int i = 0; i < array.Length; i++)
        {
            Console.WriteLine(array[i]);
        }
        Console.WriteLine("usimg foreach loop and array 2 bfeore adding element");
        foreach (int i in arrray2)
        {
            Console.WriteLine(arrray2[i]);
        }
        Console.WriteLine($"firts elelemt of array is {array[0]}");
        Console.WriteLine($"last elelemt of array is {arrray2[arrray2.Length - 1]}");
        Console.WriteLine($"length of array1 is {array.Length}");
        Console.WriteLine($"length of array2 is {arrray2.Length}");

        Console.WriteLine($"the array1 at index {array.Length - 1} value {array[array.Length - 1]} is update to {array[array.Length - 1] = 20}");
    }
}